using System;
using UnityEngine;

namespace UI
{
    public class EscapeScrollView : Menu
    {
        private void Update()
        {
            // if (Input.GetButtonDown("Cancel") && Mm.instance.IsEmpty())
            // {
            //     Push();
            // }
        }
    }
}
